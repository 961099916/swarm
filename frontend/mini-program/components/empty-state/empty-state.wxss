/* File: /Users/zhangjiahao/IdeaProjects/swarm/frontend/mini-program/components/empty-state/empty-state.wxss */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 100rpx 48rpx;
  text-align: center;
}
.empty-icon {
  border-radius: var(--radius-md);
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 32rpx;
  border: 1rpx solid var(--glass-border);
  color: var(--text-muted);
}
.empty-icon t-icon {
  color: var(--text-muted) !important;
}
.empty-label {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 12rpx;
}
.empty-desc {
  font-size: 24rpx;
  color: var(--text-muted);
  line-height: 1.6;
  max-width: 500rpx;
}
.empty-actions {
  margin-top: 32rpx;
  display: flex;
  justify-content: center;
  width: 100%;
}

/* 浮动呼吸（空状态图标/装饰用） */
@keyframes float-breathe {
  0%, 100% { transform: translateY(0); }
  50%      { transform: translateY(-12rpx); }
}
.float-breathe {
  animation: float-breathe 3s ease-in-out infinite;
}
