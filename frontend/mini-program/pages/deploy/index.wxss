/* File: /Users/zhangjiahao/IdeaProjects/swarm/frontend/mini-program/pages/deploy/index.wxss */

/* ── 全局内联样式替代类（阿里规范：结构与样式分离） ── */

/* 页面副标题 */
.page-subtitle {
  font-size: 22rpx;
  color: var(--text-muted);
  display: block;
  margin-bottom: 24rpx;
}

/* 按钮图标 */
.btn-icon-add {
  font-size: 24rpx;
  margin-right: 6rpx;
}

/* 品牌色数字强调 */
.num-brand {
  color: var(--brand);
  font-weight: 700;
}

/* 弹窗图标 */
.picker-icon-close {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80rpx 0;
}
.picker-empty-text {
  font-size: 24rpx;
  color: var(--text-muted);
}

/* 列表项头像容器 */
.agent-avatar-icon-wrap {
  width: 56rpx;
  height: 56rpx;
  font-size: 28rpx;
  flex-shrink: 0;
}
.agent-avatar-icon {
  font-size: 28rpx;
}

/* 勾选图标 */
.picker-icon-check {
  font-size: 24rpx;
  color: #FFFFFF;
}

.deploy-container {
  padding: 24rpx;
  padding-bottom: calc(160rpx + env(safe-area-inset-bottom));
  background: var(--bg-root);
  color: var(--text);
  min-height: 100vh;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  transition: background 0.3s ease;
  animation: fade-up 0.35s ease-out both;
}

.deploy-container.theme-light { background: var(--bg-root); }

.form-panel { padding: 28rpx; border-radius: var(--radius-lg); }

.form-body { display: flex; flex-direction: column; gap: 36rpx; }

.form-group { display: flex; flex-direction: column; gap: 14rpx; }

.form-label { font-size: 26rpx; font-weight: 600; color: var(--text-primary); }
.form-label-row { display: flex; justify-content: space-between; align-items: center; }
.form-link { font-size: 24rpx; font-weight: 600; color: var(--brand); }
.form-hint { font-size: 20rpx; color: var(--text-muted); }
.form-placeholder { color: var(--text-placeholder); }

/* TDesign 组件主题覆盖 */
.td-input-custom {
  --td-input-bg-color: var(--glass-bg);
  --td-input-border-color: var(--glass-border);
  --td-input-text-color: var(--text-primary);
  --td-input-placeholder-color: var(--text-placeholder);
  border-radius: var(--radius-lg);
}
.td-textarea-custom {
  --td-textarea-background-color: var(--glass-bg);
  --td-textarea-border-color: var(--glass-border);
  --td-textarea-text-color: var(--text-primary);
  --td-textarea-placeholder-color: var(--text-placeholder);
  --td-textarea-border-radius: var(--radius-lg);
}

/* 表单校验错误态 */
.form-group.has-error {
  border-color: var(--danger) !important;
  box-shadow: 0 0 0 3rpx var(--danger-subtle);
  border-radius: var(--radius-md);
  padding: 14rpx;
}
.field-error-text {
  font-size: 20rpx;
  color: var(--danger);
  margin-top: 4rpx;
  display: block;
}

/* ── 智能体芯片展示 ── */
.selected-agents-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 16rpx;
}
.agent-chip {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 12rpx 8rpx 8rpx;
  border-radius: var(--radius-full);
  background: var(--brand-subtle);
  border: 1rpx solid var(--brand-border);
}
.agent-chip-avatar {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  background: var(--brand-gradient);
  color: #FFFFFF;
  font-size: 18rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.agent-chip-name {
  font-size: 22rpx;
  font-weight: 600;
  color: var(--brand);
  max-width: 120rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.agent-chip-remove {
  width: 28rpx;
  height: 28rpx;
  border-radius: 50%;
  background: rgba(0,0,0,0.15);
  color: var(--text-muted);
  font-size: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

/* 操作按钮行 */
.agent-actions-row {
  display: flex;
  gap: 12rpx;
}

/* ── 三段式智能体选择器弹窗 ── */
.t-popup--bottom {
  padding-bottom: 0 !important;
}

.picker-sheet {
  display: flex;
  flex-direction: column;
  max-height: 92vh;
  background: var(--bg-surface-solid);
  border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  border-top: 1rpx solid var(--glass-border);
}

/* ── 上区：标题 + 搜索 ── */
.picker-top {
  flex-shrink: 0;
  background: var(--bg-surface-solid);
  border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  overflow: hidden;
  position: relative;
  z-index: 2;
  box-shadow: 0 4rpx 24rpx rgba(0,0,0,0.08);
}

.picker-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 32rpx 0;
}

.picker-title {
  font-size: 34rpx;
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: 0.5rpx;
}

.picker-close-btn {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: var(--glass-bg);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: background 0.2s;
}
.picker-close-btn:active {
  background: var(--bg-surface-hover);
}

.picker-search-wrap {
  padding: 16rpx 24rpx 12rpx !important;
}
.picker-icon-close-inner {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
  font-size: 24rpx;
  color: var(--text-muted);
}

/* ── 中区：列表区 ── */
.picker-middle {
  flex: 1;
  min-height: 0;
  overflow: hidden;
  background: var(--bg-surface);
  position: relative;
  z-index: 1;
}

.picker-list {
  height: 100%;
  overflow-y: scroll;
  padding: 12rpx 28rpx 20rpx;
  -webkit-overflow-scrolling: touch;
  box-sizing: border-box;
}

.picker-agent-item {
  display: flex;
  align-items: center;
  gap: 14rpx;
  padding: 18rpx 20rpx;
  border-radius: var(--radius-md);
  margin-bottom: 6rpx;
  transition: all 0.2s;
  background: transparent;
  border: 1rpx solid transparent;
}
.picker-agent-item:active {
  background: var(--bg-surface-hover);
  transform: scale(0.98);
}
.picker-agent-item.selected {
  background: var(--brand-subtle);
  border-color: var(--brand-border);
}

.picker-agent-info { flex: 1; min-width: 0; }
.picker-agent-name {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--text-primary);
  display: block;
  line-height: 1.4;
}
.picker-agent-role {
  font-size: 21rpx;
  color: var(--text-muted);
  margin-top: 4rpx;
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  line-height: 1.4;
}

.picker-check {
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  background: var(--brand-gradient);
}

/* ── 下区：操作栏 ── */
.picker-bottom {
  flex-shrink: 0;
  background: var(--bg-surface-solid);
  position: relative;
  z-index: 2;
  box-shadow: 0 -6rpx 28rpx rgba(0,0,0,0.08);
}

.picker-bottom-inner {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx 32rpx calc(20rpx + env(safe-area-inset-bottom));
}

.picker-selected-count {
  font-size: 24rpx;
  color: var(--text-muted);
  white-space: nowrap;
  flex-shrink: 0;
}

.agent-list { display: flex; flex-direction: column; gap: 14rpx; }
.agent-item { padding: 20rpx 24rpx; display: flex; align-items: center; gap: 14rpx; border-radius: var(--radius-md); }
.agent-item.selected { border-color: var(--brand-border); background: var(--brand-subtle); box-shadow: var(--shadow-glow); }
.agent-avatar { width: 72rpx; height: 72rpx; border-radius: 50%; background: var(--brand-subtle); display: flex; align-items: center; justify-content: center; font-size: 32rpx; flex-shrink: 0; }
.agent-info { flex: 1; min-width: 0; }
.agent-name { font-size: 24rpx; font-weight: 600; color: var(--text-primary); display: block; }
.agent-role { font-size: 20rpx; color: var(--text-muted); margin-top: 4rpx; display: block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.selected-mark { width: 36rpx; height: 36rpx; border-radius: 50%; background: var(--brand-gradient); color: #FFF; font-size: 18rpx; font-weight: 700; display: flex; align-items: center; justify-content: center; flex-shrink: 0; box-shadow: 0 0 12rpx rgba(255,107,107,0.4); }

.cost-card { padding: 28rpx; display: flex; flex-direction: column; gap: 14rpx; border-radius: var(--radius-lg); }
.cost-head { display: flex; justify-content: space-between; align-items: center; }
.cost-title { font-size: 28rpx; font-weight: 700; color: var(--text-primary); }
.cost-status { font-size: 20rpx; padding: 6rpx 20rpx; border-radius: var(--radius-full); font-weight: 600; }
.cost-status.ok { background: var(--success-subtle); color: var(--success); backdrop-filter: var(--glass-blur); -webkit-backdrop-filter: var(--glass-blur); border: 1rpx solid var(--success-border); }
.cost-status.warn { background: var(--danger-subtle); color: var(--danger); backdrop-filter: var(--glass-blur); -webkit-backdrop-filter: var(--glass-blur); border: 1rpx solid var(--danger-border); }
.cost-body { display: flex; flex-direction: column; gap: 14rpx; }
.cost-row { display: flex; justify-content: space-between; font-size: 24rpx; }
.cost-label { color: var(--text-secondary); }
.cost-minus { color: var(--danger); font-weight: 700; }
.cost-balance { font-weight: 700; }
.cost-balance.ok { color: var(--brand); }
.cost-balance.warn { color: var(--danger); }
.cost-note { font-size: 22rpx; color: var(--text-secondary); }
.cost-note.warn { color: var(--danger); }

/* 弹窗毛玻璃底部面板 */
.modal-sheet { padding: 32rpx; padding-bottom: calc(40rpx + env(safe-area-inset-bottom)); display: flex; flex-direction: column; gap: 20rpx; max-height: 80vh; background: var(--bg-surface-solid); border-radius: var(--radius-xl) var(--radius-xl) 0 0; border-top: 1rpx solid var(--glass-border); overflow: hidden; }
.modal-head { display: flex; justify-content: space-between; align-items: center; }
.modal-title { font-size: 32rpx; font-weight: 700; color: var(--text-primary); }
.modal-close { font-size: 44rpx; color: var(--text-muted); padding: 0 12rpx; }
.modal-body { display: flex; flex-direction: column; gap: 20rpx; overflow-y: auto; max-height: 60vh; }

.tool-opt { flex: 1; padding: 18rpx; text-align: center; border-radius: var(--radius-md); font-size: 22rpx; background: var(--glass-bg); backdrop-filter: var(--glass-blur); -webkit-backdrop-filter: var(--glass-blur); border: 1rpx solid var(--glass-border); color: var(--text-secondary); }
.tool-opt.active { border-color: var(--brand-border); background: var(--brand-subtle); color: var(--brand); font-weight: 600; }

.save-btn { --td-button-primary-bg-color: var(--brand) !important; border-radius: var(--radius-md) !important; margin-top: 8rpx; background: var(--brand-gradient) !important; border: none !important; }

/* ── 自定义步骤流编排器 ── */
.custom-flow-panel {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

/* 步骤大卡片美化（奶油毛玻璃质感） */
.step-card {
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius-lg);
  padding: 24rpx;
  position: relative;
  box-shadow: var(--shadow-sm);
}

.step-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
  border-bottom: 1rpx solid var(--glass-border);
  padding-bottom: 12rpx;
}

.step-card-title-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.step-badge {
  background: var(--brand-gradient);
  color: #fff;
  font-size: 20rpx;
  font-weight: 700;
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.step-title-input {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  width: 250rpx;
}

.step-card-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.step-action-icon {
  color: var(--text-muted);
  font-size: 24rpx;
  padding: 8rpx;
  transition: all 0.2s ease;
}

.step-action-icon:active {
  color: var(--brand);
}

.step-action-icon.danger:active {
  color: var(--danger);
}

.step-form {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.step-field {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.step-field-label {
  font-size: 22rpx;
  color: var(--text-muted);
  font-weight: 500;
}

/* 下拉触发器和输入项奶油底色 */
.step-picker-trigger {
  background: var(--bg-surface);
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius-md);
  padding: 16rpx 20rpx;
  font-size: 26rpx;
  color: var(--text-primary);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.step-textarea {
  background: var(--bg-surface);
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius-md);
  padding: 16rpx 20rpx;
  font-size: 26rpx;
  color: var(--text-primary);
  height: 100rpx;
  width: auto;
}

.branches-container {
  border: 1rpx dashed var(--glass-border);
  border-radius: var(--radius-md);
  padding: 16rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.branch-item {
  background: var(--bg-surface);
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius-sm);
  padding: 16rpx;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  position: relative;
}

.branch-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.branch-title {
  font-size: 22rpx;
  color: var(--brand);
  font-weight: 600;
}

.branch-remove {
  font-size: 20rpx;
  color: var(--danger);
  padding: 4rpx 10rpx;
}

.branch-input {
  background: var(--bg-surface);
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius-sm);
  padding: 10rpx 16rpx;
  font-size: 24rpx;
  color: var(--text-primary);
}

.add-branch-btn {
  border: 1rpx dashed var(--brand-border);
  background: var(--brand-subtle);
  color: var(--brand);
  font-size: 22rpx;
  font-weight: 600;
  text-align: center;
  padding: 12rpx;
  border-radius: var(--radius-sm);
}

.add-step-btn {
  border: 2rpx dashed var(--glass-border);
  background: var(--bg-surface-solid);
  color: var(--text-secondary);
  font-size: 26rpx;
  font-weight: 600;
  text-align: center;
  padding: 24rpx;
  border-radius: var(--radius-lg);
  margin-top: 10rpx;
}

.add-step-btn:active {
  background: var(--bg-surface-hover);
  border-color: var(--brand-border);
  color: var(--brand);
}

/* 覆盖 t-tabs 的自定义奶油暗色样式，使其完美融入项目全局配色 */
.custom-tabs {
  --td-tabs-bg-color: transparent !important;
  --td-tab-item-color: var(--text-secondary) !important;
  --td-tab-item-active-color: var(--brand) !important;
  --td-tab-track-color: var(--brand) !important;
  --td-tab-track-thickness: 4rpx !important;
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  overflow: hidden;
}

/* 部署编排新增辅助样式 */
.branch-hint { margin-top: -6rpx !important; }
.branch-next-hint { margin-top: 4rpx !important; }
.step-label-spacing { margin-top: 16rpx !important; }

.btn-flow-preview {
  margin-top: 20rpx !important;
  width: 100% !important;
  border: 2rpx dashed var(--glass-border) !important;
  background: var(--bg-surface-solid) !important;
  color: var(--brand) !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  gap: 10rpx !important;
}

/* 拓扑预览面板与 Canvas 美化 */
.flow-preview-sheet {
  height: 75vh !important;
}
.flow-preview-close {
  font-size: 24rpx;
  color: var(--text-muted);
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
}
.flow-preview-middle {
  padding: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  height: 100%;
}
.flow-preview-canvas {
  width: 100%;
  height: 100%;
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius-md);
  background: var(--bg-root);
}
